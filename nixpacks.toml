# nixpacks.toml — Railway build configuration
# Installs LibreOffice for DOCX→PDF conversion

[phases.setup]
nixPkgs = ["libreoffice", "nodejs_20", "fontconfig"]

[phases.install]
cmds = ["npm ci --only=production"]

[start]
cmd = "node index.js"
